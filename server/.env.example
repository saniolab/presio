SUPABASE_URL=https://<your-project>.supabase.co
SUPABASE_SERVICE_ROLE_KEY=<your-service-role-key>
PORT=3001
# Bearer key accepted only for trusted server-to-server session creation/update.
PRESIO_SERVICE_API_KEY=
# Shared with a trusted external presentation system for short-lived handoff JWTs.
PRESIO_HANDOFF_JWT_SECRET=
PRESIO_HANDOFF_JWT_ISSUER=https://courses.example.com
# Comma-separated browser origins allowed for cross-origin API/socket access.
# Leave unset when the client is served from the same origin as the server.
# ALLOWED_ORIGIN=https://your-app.example.com

# Public origin used in generated links (canonical/og:url tags, /api/present
# handoff URLs, OpenAPI self-references). Set this on any deployment served from
# a known domain: without it the origin is taken from the request's Host header,
# which a client can spoof. Leave unset for local/LAN use, where the server is
# legitimately reached as localhost, a LAN IP, or a hostname.
# PUBLIC_BASE_URL=https://your-app.example.com

# Fully local / offline mode: swap Supabase for a bundled SQLite database and
# filesystem storage, so none of the above is needed (see server/local/ and
# local.docker-compose.yml). Login and sync-by-account are unavailable.
# PRESIO_MODE=local
# Where local mode keeps its SQLite file and uploaded PDFs. Default: ./data
# LOCAL_DATA_DIR=./data
# Set to false when no reverse proxy sits in front, so clients can't spoof
# their rate-limit IP via X-Forwarded-For. Default: trust one hop (Traefik).
# TRUST_PROXY=false

# Error tracking (Sentry or self-hosted GlitchTip). Leave empty to disable.
# SENTRY_DSN=https://<key>@<host>/<project>
# SENTRY_TRACES_SAMPLE_RATE=0.1
