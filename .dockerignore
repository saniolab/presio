node_modules
client/node_modules
server/node_modules
client/dist
.env
*.env
**/.env
.git
data
# Runtime data dirs must not enter the image. Vendored Kong/Postgres init
# files under deploy/volumes are copied in (see Dockerfile) so prod can run
# from compose + .env without a git checkout.
deploy/volumes/db/data
deploy/volumes/storage
proxy
docker-compose.yml
