SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_DEFAULT_KEY=your-publishable-key
# Bearer key accepted only for trusted server-to-server session creation/update.
PRESIO_SERVICE_API_KEY=
# Shared with a trusted external presentation system for short-lived handoff JWTs.
PRESIO_HANDOFF_JWT_SECRET=
PRESIO_HANDOFF_JWT_ISSUER=https://courses.example.com
# Comma-separated browser origins allowed for cross-origin API/socket access.
# Leave unset when the client is served from the same origin as the server.
# ALLOWED_ORIGIN=https://your-app.example.com

# Public origin used in generated links (canonical/og:url tags, /api/present
# handoff URLs). Set this when serving from a known domain — otherwise the
# origin comes from the client-controlled Host header.
# PUBLIC_BASE_URL=https://your-app.example.com

# Error tracking (Sentry or self-hosted GlitchTip). Leave empty to disable.
# SENTRY_DSN is read by the server; VITE_SENTRY_DSN is baked into the client.
# SENTRY_DSN=https://<key>@<host>/<project>
# VITE_SENTRY_DSN=https://<key>@<host>/<project>

# OAuth buttons (baked in at build time). Unset GitHub = show; Authentik = hide.
# VITE_AUTH_GITHUB=true
# VITE_AUTH_AUTHENTIK=false

# Marketing chrome (homepage pitch, "Enjoying Presio?", install prompt, wordmark).
# Default on. Set false for a white-label embed. Rebuild the client after changing.
# VITE_BRANDING=true
