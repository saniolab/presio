# Staging deployment — a second Presio app container built from the working
# tree, routed by the shared Traefik proxy at ${STAGING_HOST}. It reuses the
# production Supabase stack (same .env), so no extra services are needed:
#
#   docker compose -f docker-compose.staging.yml up -d --build
#
# Sessions created here land in the shared DB but expire after 24h. Sentry is
# intentionally not configured so staging errors don't pollute prod tracking.

name: presio-staging

services:
  presio-staging:
    container_name: presio-staging
    build:
      context: .
      dockerfile: deploy/Dockerfile
      args:
        VITE_SUPABASE_URL: ${SUPABASE_PUBLIC_URL}
        VITE_SUPABASE_PUBLISHABLE_DEFAULT_KEY: ${ANON_KEY}
        VITE_AUTH_GITHUB: ${VITE_AUTH_GITHUB:-${GITHUB_ENABLED:-true}}
        VITE_AUTH_AUTHENTIK: ${VITE_AUTH_AUTHENTIK:-${AUTHENTIK_ENABLED:-false}}
        # The deploying commit, so /healthz identifies exactly what is running
        # here. deploy-staging.yml sets this and then waits for that value to
        # appear before calling the deploy good.
        APP_VERSION: ${APP_VERSION:-dev}
    restart: unless-stopped
    networks:
      web:
    environment:
      SUPABASE_URL: ${SUPABASE_PUBLIC_URL}
      SUPABASE_SERVICE_ROLE_KEY: ${SERVICE_ROLE_KEY}
      PORT: 3001
      ALLOWED_ORIGIN: https://${STAGING_HOST:-staging.presio.xyz}
      # Pin the origin used in generated links (canonical/og:url, handoff URLs)
      # instead of deriving it from the client-controlled Host header. Staging
      # sits behind Traefik on a known hostname, so the value is knowable here.
      PUBLIC_BASE_URL: https://${STAGING_HOST:-staging.presio.xyz}
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=web"
      - "traefik.http.routers.presio-staging.rule=Host(`${STAGING_HOST:-staging.presio.xyz}`)"
      - "traefik.http.routers.presio-staging.entrypoints=websecure"
      # Serve TLS from the proxy's default certificate (Cloudflare Origin CA).
      - "traefik.http.routers.presio-staging.tls=true"
      - "traefik.http.services.presio-staging.loadbalancer.server.port=3001"

networks:
  # Shared with the Traefik proxy (created once per host: `docker network create web`).
  web:
    external: true
